eslint .    # check linting errors of all files in project

# DEV Commands as seen in package.json

npm start     # = sudo node server
npm dev       # = nodemon server
npm check     # = eslint .

# Updating edits

git pull    # also might have to npm install after pulling
